<%= link_to image_tag('logo.png'), root_path, :class => 'brand' %>
<ul class="nav" style="float:left">
  <% if user_signed_in? %>
    <li><%= link_to '用户中心', user_path(current_user) %></li>
  <% else%>
  <% end %>
</ul>

<ul class="nav">

  <% if user_signed_in? %>
    <li class="nav-li">
    <%= link_to '登出', destroy_user_session_path, :method=>'delete',:class=>"btn nav-a" %>
    </li>
  <% else %>
    <li class="nav-li">
    <%= link_to '登录', new_user_session_path,:class=>"btn nav-a" %>
    </li>
  <% end %>
  <% if user_signed_in? %>
    <li class="nav-li">
    <%= link_to '修改账户', edit_user_registration_path,:class=>"btn nav-a" %>
    </li>
    <% if current_user.has_role? :admin %>
      <li class="nav-li">
      <%= link_to '管理用户', users_path,:class=>"btn nav-a" %>
      </li>
    <% end %>
  <% else %>
    <li class="nav-li">
    <%= link_to '注册', new_user_registration_path,:class=>"btn nav-a" %>
    </li>
  <% end %>
</ul>
